import ClientLoginClient from "./client";

export const metadata = {
  title: "Client Login — Fancy Digitals",
  description: "Access your website dashboard",
};

export default function ClientLoginPage() {
  return <ClientLoginClient />;
}